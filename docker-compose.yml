version: '3.8'

services:
  frontend:
    image: react-app:latest
    ports:
      - "3000:3000"
    restart: unless-stopped

  backend:
    image: node-api:latest
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped